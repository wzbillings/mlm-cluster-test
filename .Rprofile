source("renv/activate.R")
source("~/.Rprofile")

# When using renv, install packages with pak
# Needs to be off when we expect someone to use renv::restore()
#options(renv.config.pak.enabled = FALSE)
